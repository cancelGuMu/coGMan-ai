# 审核 Agent 复审记录

## 2026-04-25

- 审核结论：不通过
- 问题列表：
  - 当前仅完成展示页、步骤一、步骤二和基础 API，步骤三到步骤九仍为占位状态，无法满足完整项目发布门禁。
  - 未完成真实用户全链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档` 的全流程复审。
  - 当前 AI 能力仍为本地模拟接口，尚未接入真实模型服务。
- 发布建议一句话：当前版本适合作为开发基础版本继续迭代，不可对外视为正式可发布版本。

## 2026-04-27

- 审核结论：不通过
- 问题列表：
  - 本轮已将“创作中心”接成真实页面，并接入步骤一、步骤二，但步骤三到步骤九仍是页面骨架与占位区，尚未完成真实功能闭环。
  - 已完成前端构建校验，但尚未执行“新建项目 -> 步骤一编辑保存 -> 步骤二编辑保存 -> 再次打开回显 -> 删除/归档”的完整人工链路复审。
  - 创作中心当前默认载入最近项目并支持新建项目，但尚未补充对应的发布级 UI/交互验收记录。
- 发布建议一句话：本轮适合继续在创作中心上迭代步骤三到步骤九，暂不作为正式发布版本。

## 2026-04-28

- 审核结论：不通过
- 问题列表：
  - 已完成本轮需求映射：创作中心从单页下拉改为 `/create-center/:stepId` 九个独立页面，步骤导航改为路由跳转，步骤一/二保留编辑与保存入口，步骤三到步骤九为独立页面骨架。
  - 已通过前端构建：`npm run build` 成功。
  - 已完成九个步骤 URL 冒烟：`topic-planning`、`script-creation`、`storyboard-design`、`character-image`、`image-to-video`、`voice-subtitle`、`editing-export`、`distribution`、`data-review` 均返回 HTTP 200。
  - 未完成发布级门禁：本轮没有启动后端并执行“新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档”的完整真实用户链路。
  - 风险说明：为修复原 `App.tsx` 编码损坏并恢复可构建状态，前端入口已重写为较简洁实现，旧首页部分视觉展示内容不再完整保留，需要后续按产品视觉稿复核。
- 发布建议一句话：本轮满足“九步拆成九个页面”的结构要求，但未完成完整真实链路复审，暂不建议作为正式发布版本。

## 2026-04-28 首页恢复复审

- 审核结论：不通过
- 问题列表：
  - 已按用户要求恢复首页为完整产品首页结构：顶部导航、Hero 工作台预览、统计卡、九步流程展示、项目工作台预览、数据看板、底部 CTA、页脚均已回到首页。
  - 已保留上一轮需求：创作中心仍使用 `/create-center/:stepId` 九个独立页面路由，没有回退为单页锚点下拉。
  - 已通过前端构建：`npm run build` 成功。
  - 已完成轻量路由冒烟：`/`、`/create-center/topic-planning`、`/create-center/storyboard-design`、`/create-center/data-review` 均返回 HTTP 200。
  - 未完成发布级门禁：本轮没有启动后端并执行完整真实用户链路复审。
  - 风险说明：由于原源码没有可用 Git HEAD 可恢复，首页是基于现有样式和素材重建的“原首页结构恢复”，可能仍需人工比对旧截图做像素级修正。
- 发布建议一句话：首页结构已恢复且构建通过，但未完成真实链路和视觉逐像素复核，暂不建议作为正式发布版本。

## 2026-04-28 ???????????

- ????????
- ?????
  - ?? Codex ????? sourcemap ?????????????`apps/web/src/App.tsx` ??? 93361 ?????????? `HomePage`?`dashboardRangeOptions`?`workflowShowcase` ?????????
  - ????????? `apps/web/src/styles.css` ???????? `apps/web/src/data.ts` ???????`apps/web/src/payload.ts` ????????
  - ?????????????????????????? `/create-center/:stepId` ???`/create-center` ????????????????????????????????
  - ????????`npm run build` ???
  - ????? URL ???`/`?`/create-center` ???? `/create-center/<stepId>` ??? HTTP 200?
  - ??????????????????????? -> ??/?? -> ?? -> ?? -> ?? -> ????? -> ???? -> ??/?????????????
- ???????????????????????????????????????????????????????

## 2026-04-28 项目卡片液态玻璃恢复复审

- 审核结论：不通过
- 问题列表：
  - 已按用户要求读取项目核心记忆，并确认后续修改需要提交并上传 GitHub。
  - 已将项目工作台卡片背景恢复为 `var(--liquid-glass)` 液态玻璃效果，同时保留卡片 hover 放大与外侧边缘荧光。
  - 已通过前端构建：`npm run build` 成功。
  - 未完成发布级门禁：本轮没有执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮样式恢复可进入 GitHub 留痕，但仍不建议作为正式发布版本。

## 2026-04-28 数据看板下拉框点击区域复检

- 审核结论：不通过
- 问题列表：
  - 已完成需求映射：将数据看板时间下拉框的原生 select 扩展为覆盖整个胶囊控件，右侧三角区域也可触发下拉。
  - 已完成基础验证：`npm run build:web` 成功，`git diff --check` 通过。
  - 已完成敏感提交检查：未发现项目核心记忆、环境变量、node_modules、dist、tsbuildinfo 等禁入内容进入暂存范围。
  - 未完成发布级全链路门禁：本次为前端样式微调，未执行“新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档”的完整真实用户链路复检。
- 发布建议一句话：本次修复适合作为前端交互小迭代提交，但不应单独视为完整发布门禁通过。

## 2026-04-28 首页展示图填充复审

- 审核结论：不通过
- 问题列表：
  - 已按用户要求读取项目核心记忆，并确认本轮需要敏感检查、本地小提交、上传 GitHub。
  - 已完成需求映射：将首页 Hero 中间展示页圆角矩形内的 `hero-stage-main.png` 改为 `cover` 填满，并启用圆角裁切，避免图片缩在框内留边。
  - 已通过前端构建：`npm run build` 成功。
  - 已完成敏感内容检查：本次相关样式文件未发现 GitHub token、API key、私钥等敏感内容；`mask-image` 为 CSS 属性误报。
  - 未完成发布级全链路门禁：本轮为首页视觉样式微调，未执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本次样式修正可作为前端小迭代提交，但不应单独视为完整发布门禁通过。

## 2026-04-28 十一步工作流内容口径复审

- 审核结论：不通过
- 问题列表：
  - 已按《项目步骤功能说明.md》将前端步骤导航、首页可拖动 3D 工作流展示、创建中心文案、步骤一标题与 README 口径更新为 11 个步骤：故事架构、剧本创作、资产设定、分镜规划、提词生成、画面生成、质检返工、视频生成、音频字幕、剪辑成片、发布复盘。
  - 已同步更新前后端 `StepId` 枚举与后端步骤流转，并为旧 9 步 ID 增加兼容映射，避免已有本地项目数据读取失败。
  - 已通过 `npm run build:web`、`git diff --check`、旧文案残留扫描、后端旧数据兼容验证，以及 `/`、`/create-center`、`/create-center/story-structure`、`/create-center/publish-review` 路由 HTTP 200 冒烟。
  - 未完成发布级门禁：本轮未执行真实用户全链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`，因此不能判定为正式发布通过。
- 发布建议一句话：本轮适合作为“9 步口径迁移到 11 步口径”的内容与路由基础提交，但仍需后续补齐真实用户全链路复审后再作为发布版本。

## 2026-04-28 主要功能页面开发明细表复审

- 审核结论：不通过
- 问题列表：
  - 已根据《项目步骤功能说明.md》生成《主要功能页面开发明细表.md》，按 11 步拆解页面目标、核心产物、上游依赖、下游服务对象、模块明细、输入输出、按钮交互、数据实体、接口能力、优先级和后续落地阶段。
  - 已对齐当前 README 与前端 `workflowSteps` 的 11 步口径，未继续沿用旧 9 步页面规划。
  - 已完成文档级审查：新增文档不包含项目核心记忆中的密钥内容，不包含 `.env`、本地数据、构建产物或依赖目录。
  - 未完成发布级门禁：本轮是功能规划文档，不涉及真实页面链路，因此未执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮文档可作为后续主要功能页面开发蓝图提交，但不代表实际功能页面已经完成。

## 2026-04-28 工作流展示 Hover 放大移除复审

- 审核结论：不通过
- 问题列表：
  - 已按用户要求去掉首页可拖动工作流展示卡片在鼠标悬停时的前移与放大效果，保留原有样式结构、拖动交互、边框与光影反馈。
  - 已同步移除响应式布局下工作流卡片 hover 上移效果，避免移动/窄屏布局出现悬停位移。
  - 已通过 `npm run build:web` 与 `git diff --check` 验证。
  - 未完成发布级门禁：本轮为展示区样式微调，未执行完整真实用户全链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮可作为首页工作流展示交互微调提交，但仍不单独视为正式发布门禁通过。

## 2026-04-28 主要功能页面小任务拆分清单复审

- 审核结论：不通过
- 问题列表：
  - 已根据《主要功能页面开发明细表.md》生成《主要功能页面小任务拆分清单.md》，将 11 步功能页面拆成全局基础、创作中心骨架、步骤 01-11、后端接口、测试验收等独立小任务。
  - 已在清单顶部明确“每次只完成一个小任务”的执行规则，并给每个任务补充编号、范围、交付物和验收点。
  - 已给出推荐执行顺序，并建议下一次只做 `G-001：建立小任务状态记录文件`。
  - 本轮为规划文档拆分，不涉及实际页面代码，因此未执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮文档可作为后续单任务迭代的任务池，但不代表任何具体功能页面已经完成。

## 2026-04-28 G-001 到 G-010 基础任务复审

- 审核结论：不通过
- 问题列表：
  - 已完成 G-001：新增《主要功能页面任务状态.md》，用于记录任务状态、交付物、验证方式和提交号。
  - 已完成 G-002 到 G-004：新增《主要功能页面现状盘点.md》，梳理当前 11 步路由映射、前端类型缺口和后端模型缺口。
  - 已完成 G-005 到 G-006：新增 `workflowStepDetails`、`getWorkflowStepDetail`、`StepCompletionStatus` 与 `stepCompletionStatusMeta`，为后续步骤导航和页面提示提供统一数据源。
  - 已完成 G-007 到 G-010：新增 `SaveStatusBadge`、`EmptyStatePanel`、`ErrorNotice`、`SectionHeader` 和独立组件样式文件。
  - 已通过 `npm run build:web` 验证；本轮未接入真实页面链路，未执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮可作为主要功能页面基础设施提交，但仍不代表具体业务功能页面已经完成。

## 2026-04-29 G-011 到 S01-005 批量任务复审

- 审核结论：不通过
- 问题列表：
  - 已完成 30 个小任务：`G-011` 到 `G-020`、`C-001` 到 `C-015`、`S01-001` 到 `S01-005`。
  - 已新增通用双栏、三栏、版本记录、AI 生成按钮组、导入按钮、下一步按钮、步骤状态圆点和加载骨架组件。
  - 已扩展步骤一前后端数据结构，补充项目基础信息、核心故事标题、世界观、主线目标、人物关系、季纲和导入解析状态字段。
  - 已将创作中心加载态替换为骨架屏，并在顶部切换项目区域补充创建新项目入口，步骤导航接入状态圆点。
  - 已补充自动保存设计稿、移动端导航方案和创作中心验收清单模板。
  - 已通过 `npm run build:web` 与 `git diff --check`。
  - 未完成发布级门禁：本轮仍未执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮可作为创作中心公共基础与步骤一增强批量提交，但仍需后续真实链路复审后再作为发布版本。

## 2026-04-29 S01-006 到 S03-003 批量任务复审

- 审核结论：不通过
- 问题列表：
  - 已完成 40 个小任务：`S01-006` 到 `S01-020`、`S02-001` 到 `S02-022`、`S03-001` 到 `S03-003`。
  - 步骤一补齐世界观、主线目标、人物关系、单集大纲、连续性检查、保存和进入剧本创作校验。
  - 步骤二补齐集数选择、当前集上下文、素材导入文件名回显、剧本文本编辑、格式标记、节奏节点、改写、一致性检查、版本记录、版本恢复和导出。
  - 步骤三新增资产库类型、后端 `step_three` 存储字段和三栏资产设定页面骨架。
  - 已通过 `npm run build:web` 与 `git diff --check`。
  - 未完成发布级门禁：本轮没有执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮可作为步骤一完整闭环、步骤二增强和步骤三起步提交，但仍需后续真实链路复审后再作为发布版本。

## 2026-04-29 S03-004 到 S06-014 批量任务复审

- 审核结论：不通过
- 问题列表：
  - 已完成 80 个小任务：`S03-004` 到 `S06-014`。
  - 步骤三资产设定已具备候选提取、候选勾选入库、新增角色、角色/场景/道具数据结构、风格板、一致性规则、提示词模板和保存接口。
  - 步骤四分镜规划已具备分镜数据结构、后端存储、自动拆镜、新增镜头、镜头卡片、角色/场景/道具引用、镜头参数、总时长统计、任务预览和保存接口。
  - 步骤五提词生成已具备提示词库数据结构、后端存储、镜头筛选、T2I/I2V 单镜与批量模拟生成、负面词/参数模板、批量替换、锁定词、复制提示词和保存接口。
  - 步骤六画面生成已具备画面素材数据结构、后端存储、单镜/批量占位生成、候选图网格、设为首帧/关键帧、废弃、筛选、元数据展示和复制图片提示词。
  - 已通过 `npm run build:web` 与 `git diff --check`。
  - 未完成发布级门禁：本轮没有执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮可作为步骤三到步骤六的模拟生产链路提交，但仍需后续真实链路复审和更多细节交互后再作为发布版本。

## 2026-04-29 S06-015 到 S11-017 剩余任务收口复审

- 审核结论：不通过
- 问题列表：
  - 已完成剩余 92 个 S 类小任务：`S06-015` 到 `S06-018`、`S07-001` 到 `S07-018`、`S08-001` 到 `S08-017`、`S09-001` 到 `S09-018`、`S10-001` 到 `S10-018`、`S11-001` 到 `S11-017`。
  - 步骤六补齐重生成、局部重绘占位、入选素材包和进入质检校验。
  - 步骤七新增质检返工完整模拟工作台，覆盖质检报告、返工任务、单/批量质检、标记通过、导出报告和未通过素材拦截。
  - 步骤八新增视频生成工作台，覆盖视频候选、动作运镜参数、参考素材绑定、失败原因、重生成、最终片段和完整性检查。
  - 步骤九新增音频字幕工作台，覆盖台词提取、配音/旁白、音频播放占位、字幕时间轴、字幕样式、音效、BGM、混音和口型同步任务。
  - 步骤十新增剪辑成片工作台，覆盖自动编排、时间线、转场、节奏点、对齐检查、导出任务、封面候选、发布素材包和发布校验。
  - 步骤十一新增发布复盘工作台，覆盖发布文案、平台适配、发布记录、数据导入占位、指标汇总、留存分析、评论摘要、复盘报告、优化任务、下一集建议和项目完结状态。
  - 已通过 `npm run build:web`、`python -m compileall apps/api/src/app` 与 `git diff --check`。
  - 未完成发布级门禁：本轮是模拟生产闭环与页面收口，仍未执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`，也未接入真实视频、音频、字幕、上传和导出引擎。
- 发布建议一句话：本轮可作为 11 步主要功能页面的完整模拟闭环提交，但正式发布前仍需要真实链路测试、浏览器视觉验收和真实媒体服务接入评估。

## 2026-04-29 AI 模型能力与提示词约束文档复审

- 审核结论：不通过
- 问题列表：
  - 已根据项目核心记忆、11 步工作流说明、现有前后端数据结构和当前 AI 接口，新增 `docs/ai-model-capability-prompts.md`，覆盖模型能力分工、全局系统约束、通用输出 envelope、11 步 AI 功能提示词模板、跨步骤安全规则和后端落地建议。
  - 已完成文档级敏感信息检查：新增文档未发现 API key、私钥、令牌模式或真实密钥内容。
  - 已通过基础验证：`git diff --check` 成功，`npm run build:web` 成功。
  - 本轮为提示词和约束文档产出，不涉及真实 AI 任务接口落地，也未执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮文档可作为后续 Prompt Registry 和真实 AI 接口接入蓝图，但不能视为发布级功能门禁通过。

## 2026-04-29 AI 提示词注册表部署与工具检查复审

- 审核结论：不通过
- 问题列表：
  - 已将 AI 模型能力与提示词约束部署到后端 `apps/api/src/app/prompt_registry.py`，覆盖 11 个步骤共 40 个提示词任务，并把现有步骤一、步骤二文本生成入口接入注册表。
  - 已新增 `/api/ai-tools/diagnostics` 诊断接口，返回提示词注册表覆盖情况、模式映射和 DeepSeek / 图片生成 / MiniMax 视频通道的配置状态，且不暴露任何密钥值。
  - 已完成本地验证：`python -m compileall apps/api/src/app`、`npm run build:web`、`git diff --check` 通过；真实启动 FastAPI 后 `/healthz` 与 `/api/ai-tools/diagnostics` 均返回 HTTP 200。
  - 已完成 AI 工具检查：DeepSeek 文本通道完成一次极短真实调用；图片生成与视频生成完成本地契约模拟，确认请求组装、模型名、参数边界和返回解析可用。本轮未实际触发图片/视频外部生成，以避免无意义消耗额度。
  - 未完成发布级门禁：本轮未执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`，也未对图片/视频供应商做付费真实生成验收。
- 发布建议一句话：本轮已完成 AI 提示词部署和基础工具可用性检查，可进入下一步真实任务接口细化，但仍不能视为发布级门禁通过。

## 2026-04-29 AI 生成按钮加载态复审

- 审核结论：不通过
- 问题列表：
  - 已按用户要求为 AI 生成期间的按钮增加临时形态：生成时显示旋转标识、生成中文案和高亮状态，生成完成或失败后通过 `finally` 恢复原按钮。
  - 已覆盖现有真实异步 AI 链路：步骤一季纲生成、步骤二角色/术语/写作指导/剧本/一致性检查、步骤六图片生成与重生成、步骤八视频生成与任务状态查询。
  - 已覆盖步骤五提示词生成的临时生成态，避免模拟生成过快导致用户看不到按钮变化。
  - 已通过 `npm run build:web` 与 `git diff --check`。
  - 未完成发布级门禁：本轮为前端交互态微调，未执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮可作为 AI 生成按钮交互态修复提交，但仍不能视为正式发布门禁通过。

## 2026-04-29 步骤一季纲生成格式问题复审

- 审核结论：不通过
- 问题列表：
  - 已定位步骤一生成“不按要求”的直接原因：前端将 AI 返回原文按换行切分，并把前几行直接写入各集 `content`，导致“好的，收到任务”“前置说明”等说明性文本被当作第 1、2、3 集内容。
  - 已收紧后端 `S01_SEASON_OUTLINE` 提示词契约，要求只返回合法 JSON，禁止寒暄、前置说明、Markdown 标题和工作过程，并要求 `episodes` 数量匹配目标集数。
  - 已在步骤一接口补充硬性格式提示，要求最终回答可被 `JSON.parse` 直接解析，不允许解释性段落。
  - 已在前端新增季纲解析器：优先解析 JSON 和多段 JSON 对象，其次解析“第 N 集 / Episode N”段落；解析失败时只保留整季纲原文，不再污染单集内容。
  - 已通过 `npm run build:web`、`python -m compileall apps/api/src/app` 与 `git diff --check`。
  - 未完成发布级门禁：本轮未再次触发真实 DeepSeek 步骤一生成，也未执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮可作为步骤一 AI 生成格式修复提交，但仍需真实 AI 返回样本与完整链路复审后再作为发布级通过。

## 2026-04-29 步骤一基础三项 AI 生成匹配问题复审

- 审核结论：不通过
- 问题列表：
  - 已定位“世界观编辑 / 主线目标 / 人物关系”三项不匹配的原因：前端此前使用本地固定模板生成，没有调用真实 AI，因此会产出与用户故事无关的近未来、隐秘组织、主角/同盟/反派占位文本。
  - 已将三项按钮接入后端文本生成通道，分别使用 `S01_WORLDVIEW`、`S01_MAIN_CONFLICT`、`S01_RELATIONSHIPS` 任务，并传入当前项目类型、受众、平台、核心故事原文和已填写字段。
  - 已收紧三项提示词注册表，要求只返回合法 JSON、保留原文专有名词和故事线索、禁止泛化模板、寒暄、前置说明和 Markdown。
  - 已新增步骤一基础信息 JSON 解析逻辑，只把匹配字段写入对应表单；格式异常时不污染字段。
  - 已将三项按钮替换为 AI 加载态按钮，生成期间显示对应“生成中”状态，完成或失败后恢复。
  - 已通过 `npm run build:web`、`python -m compileall apps/api/src/app` 与 `git diff --check`。
  - 未完成发布级门禁：本轮未实际消耗 DeepSeek 生成三项真实样本，也未执行完整真实用户链路。
- 发布建议一句话：本轮可作为步骤一基础三项 AI 生成接入修复提交，但仍需真实返回样本复审后再作为发布级通过。

## 2026-04-29 步骤一项目基础信息区块移除复审

- 审核结论：通过
- 问题列表：
  - 已按截图要求移除步骤一顶部说明文案和“项目基础信息”卡片。
  - 保留项目名称、核心故事输入、故事骨架、世界观、主线、人物关系和季纲编辑区域。
  - 已通过 `npm run build:web` 与 `git diff --check`。
- 发布建议一句话：本轮为小范围 UI 删除，可随当前步骤一修复一起发布。

## 2026-04-30 首页展示页比例调整复审

- 审核结论：有条件通过
- 问题列表：
  - 已按要求只调整首页展示页布局比例和预览缩放，不改变展示内容、文案或组件结构。
  - 已降低桌面端展示台缩放并调整 hero 左右栏比例，避免右侧 3D 工作台被视口裁切。
  - 已用 Playwright 在 2048、1440、1280、1024 宽度实测：首页横向溢出均为 0，右侧展示页完整落在视口内。
  - 已通过 `git diff --check`。
  - `npm run build:web` 未通过，阻断点为当前工作区 `apps/web/src/App.tsx` 既有类型错误：`ShotItem[]`、`DialogueLine[]`、`OptimizationTask[]` 相关空值和类型谓词问题；本轮未修改这些业务内容。
- 发布建议一句话：本轮样式调整可解决展示页裁切问题，但正式发版前仍需先修复当前 `App.tsx` 类型错误。

## 2026-04-30 首页首屏上下间距与展示页放大复审

- 审核结论：有条件通过
- 问题列表：
  - 已继续压缩主页面首屏顶部间距，将 hero 内容从垂直居中改为靠上排布，并降低 hero 最小高度。
  - 已将左侧文案整体上移，同时适当放大右侧 3D 展示页。
  - 已用 Playwright 在 2048、1440、1280 宽度实测：首页横向溢出均为 0；2048 宽右侧安全边距约 134px，1280 宽安全边距约 25px。
  - 已通过 `git diff --check`。
  - 本轮仍未重新执行 `npm run build:web` 通过验证，原因同上：当前工作区存在 `App.tsx` 既有类型错误，非本次 CSS 调整引入。
- 发布建议一句话：本轮比例调整满足“上移、放大、完整显示”的视觉目标，发版前仍需统一修复当前类型错误。
## 2026-05-03 画面生成 AI 改词并重绘流程复审

- 审核结论：不通过
- 问题列表：
  - 已将步骤六「局部重绘」改为二段式生产流程：用户在候选图下填写修改意见，前端先调用 `S06_REPAINT_PROMPT` 让 AI 生成完整英文 T2I 重绘提示词，再把修订提示词提交给 `gpt-image-2` 追加生成新候选图，原候选图不被覆盖。
  - 已在候选图卡片中新增「这张图的修改意见」输入框，并将按钮文案改为「AI改词并重绘」；按钮加载态区分「AI改词中」和「重绘生成中」两段进度。
  - 已把角色视线、道具朝向、信息载体不能正面展示给观众的约束写入 T2I 与重绘任务契约，并接入导演镜头语言规则。
  - 已补充数据模型兼容字段 `repaint_instruction`，老项目候选图会默认补空字符串，避免前端输入框失控。
  - 已通过 `npm --workspace apps/web run build`、`apps/api/.venv/Scripts/python.exe -m pytest`、`git diff --check` 和前端重绘入口静态标记检查。
  - 未完成发布级门禁：本轮未真实触发 `gpt-image-2` 重绘以避免消耗额度；浏览器自动化验证因当前 Codex IAB 后端不可用未能完成可视化截图；也未执行完整真实用户链路 `新建项目 -> 输入/导入 -> 生成 -> 编辑 -> 保存 -> 关闭再打开 -> 继续编辑 -> 删除/归档`。
- 发布建议一句话：本轮可作为画面生成重绘交互闭环提交，但正式发布前仍需在可用浏览器环境中跑完整用户链路，并至少执行一次真实重绘样本验收。

## 2026-05-03 画面重绘上下文超限与无反馈修复复审

- 审核结论：通过
- 问题列表：
  - 已定位「AI改词并重绘」按钮短暂进入「AI改词中」后恢复、画面没有变化的直接原因：前端把完整候选图对象传入 `S06_REPAINT_PROMPT`，其中 `image.url` 可能包含 base64 大图，导致后端拒绝请求并返回 `AI context exceeds budget: 73434 chars`。
  - 已将传给 AI 改词任务的图片上下文改为精简对象，只保留镜头 ID、状态、元数据、原提示词、旧重绘提示词和用户修改意见，明确省略 `image.url/base64`。
  - 已对原始提示词和旧重绘提示词增加长度兜底，避免异常长文本再次撑爆 AI 输入预算。
  - 已在每张候选图卡片内新增持久反馈区，分别展示「AI 正在改词」「已提交 gpt-image-2」「重绘完成」「失败原因」，避免错误只出现在顶部状态条后用户无法感知。
  - 已避免把 AI 生成的完整英文重绘提示词写回全局「默认修改意见」输入框，防止后续图片误用上一张图的完整 prompt。
  - 已通过 `npm --workspace apps/web run build`、`apps/api/.venv/Scripts/python.exe -m pytest` 和 `git diff --check`。
  - 未完成发布级门禁：本轮未实际消耗 `gpt-image-2` 额度生成真实重绘图；浏览器可视化链路仍需在可用浏览器环境中补测。
- 发布建议一句话：本轮修复可解决重绘改词上下文超限和无可见反馈问题，可随画面重绘闭环一起发布。

## 2026-05-03 画面重绘后端上下文预算二次修复复审

- 审核结论：通过
- 问题列表：
  - 已复查用户继续遇到上下文超限的原因：前端虽已省略 `image.url/base64`，但后端 `GenerationRequest` 会按 `project_id + target_id` 自动构造 AI 网关上下文，并从已保存项目中再次读取目标候选图、镜头、提示词和候选图列表。
  - 已修复后端上下文网关的顺序问题：此前 `_finalize_bundle` 在压缩前先执行 `validate_context`，raw context 超过 60000 字符会直接抛错，导致后续压缩逻辑没有机会执行；现在改为先清理媒体与密钥、先压缩 `target_object/relevant_context/user_edits`，再做严格预算校验。
  - 已将 `S06_REPAINT_PROMPT` 相关上下文从“20 个镜头 + 20 个提示词 + 20 张候选图”收窄为“当前目标图片 + 当前目标镜头 + 当前目标提示词 + 同镜头相关候选图”，更符合单张重绘任务。
  - 已新增专项测试，模拟 12 张含超长 base64 URL 与长提示词的候选图，确认 `S06_REPAINT_PROMPT` 生成的网关 prompt 小于 60000 且不包含 `data:image`、`url`、`;base64`。
  - 已用本地真实项目「七日祭」的 `img-reg-S01E01_SHOT_05-1777771174680` 复测，最终 `prompt_chars=14886`、`context_chars=12489`，上下文键为 `target_image_or_shot/target_shot/target_prompt/related_image_candidates/style_and_rules`，不再包含媒体 payload。
  - 已通过 `apps/api/.venv/Scripts/python.exe -m pytest`（25 passed）、`npm --workspace apps/web run build`、`git diff --check`。
  - 未完成发布级门禁：本轮未实际消耗 `gpt-image-2` 额度生成真实重绘图；浏览器可视化链路仍需在可用浏览器环境中补测。
- 发布建议一句话：本轮从后端网关根因修复 S06 重绘上下文预算问题，可覆盖用户当前的 73434 chars 超限报错。

## 2026-05-03 文档切片工作流与生成速度优化复审

- 审核结论：通过
- 问题列表：
  - 已将后端上下文网关从“按步骤读取较宽摘要”进一步优化为“按任务 ID + 目标 ID 读取局部切片”：`episode` 目标可直接定位分集，`S04_STORYBOARD_SPLIT` 只读取目标集、目标集剧本摘录、故事摘要和资产摘要。
  - 已将 `S05_T2I_PROMPT/S05_I2V_PROMPT` 从整批镜头与整批提示词摘要，收窄为当前目标镜头、邻近镜头、匹配资产、风格规则和当前镜头已有提示词，避免为单镜提词反复处理整份文档。
  - 已新增按集号边界截取剧本的逻辑，优先截取 `第N集/Episode N` 到下一集标记之间的内容，避免目标集上下文把下一集文本带入。
  - 已去掉前端步骤四/五调用里手动拼入完整剧本和完整资产库的重复 prompt，让后端 AI_CONTEXT_GATEWAY 作为唯一结构化上下文入口。
  - 已将步骤五批量提示词生成改为 2 路受控并发，将步骤六批量图片生成改为 3 路受控并发，单镜生成仍保持 1 路，兼顾速度和上游稳定性。
  - 已新增专项测试覆盖 S04/S05 目标切片，确认分镜上下文包含 `target_episode/script_or_novel_excerpt`，提示词上下文包含 `neighbor_shots/existing_prompt_for_shot`，且不再暴露整批 `shots`。
  - 已用本地真实项目「七日祭」复测：`S04_STORYBOARD_SPLIT` 网关 prompt 约 20298 chars，`S05_T2I_PROMPT` 网关 prompt 约 16650 chars，均为目标切片结构。
  - 已通过 `apps/api/.venv/Scripts/python.exe -m pytest`（26 passed）、`npm --workspace apps/web run build` 和 `git diff --check`。
  - 未完成发布级门禁：本轮未实际消耗文本模型/图片模型额度做真实批量生成耗时对比；GitHub push 仍受当前网络连接限制影响。
- 发布建议一句话：本轮将文档处理从反复输入整份文本推进为稳定的目标切片工作流，并通过受控并发降低文本与图片批量生成等待时间。

## 2026-05-03 S06 重绘前端重复上下文修复复审

- 审核结论：通过
- 问题列表：
  - 已定位用户再次遇到 `AI context exceeds budget: 72445 chars` 的原因：S06「AI改词并重绘」前端仍把导演规则、完整 shot、image 摘要和 step_five_prompt 拼入 `prompt`，后端虽然已按 target_id 切片，但这段大 prompt 会作为 `user_edits` 进入上下文。
  - 已将 S06 前端调用改为只发送目标图片 ID、目标镜头 ID、用户修改意见和极短任务边界；当前图片、镜头、提示词、同镜头候选图和风格规则全部由后端网关按 `target_id` 读取。
  - 已在后端增加任务级 `user_edits` 限额：S05/S06/S08 保留 2000 字，S04 保留 4000 字，防止旧前端或异常入口再次把完整上下文塞入用户编辑区。
  - 已新增专项测试复现旧前端大 payload，确认 S06 仍会被裁剪到 60000 字以内，且不包含 `data:image`。
  - 已用本地真实项目「七日祭」复测同一目标图：`prompt_chars=14991`、`context_chars=12594`、`user_edits_chars=157`，上下文键为 `target_image_or_shot/target_shot/target_prompt/related_image_candidates/style_and_rules`。
  - 已通过 `npm --workspace apps/web run build`、`apps/api/.venv/Scripts/python.exe -m pytest`（27 passed）和 `git diff --check`。
- 发布建议一句话：本轮彻底移除 S06 前端重复上下文，并增加后端兜底裁剪，可覆盖当前 72445 chars 超限报错。
